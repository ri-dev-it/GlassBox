# Saved Models

Trained model artifacts (`.joblib`) are generated by `ml/training/train.py`
and are NOT committed to Git (see root `.gitignore`) because they are
binary and can be large.

To regenerate:
    cd ml
    python training/train.py

This will write the trained model, preprocessing pipeline, and metadata
into this folder.
