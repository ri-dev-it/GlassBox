import { useAuth } from '../../hooks/useAuth';
export default function Settings() { const { user, logout } = useAuth(); return <div className="mx-auto max-w-3xl"><p className="text-sm font-medium text-sky-700">System</p><h1 className="mt-1 text-3xl font-bold text-[#102a4c]">Settings</h1><div className="mt-6 space-y-5">{[['Profile', [['Name', user?.full_name ?? ''], ['Email', user?.email ?? ''], ['Role', user?.role ?? '']]], ['Preferences', [['Currency', 'INR (₹)'], ['Theme', 'Light'], ['Language', 'English']]], ['System', [['Model version', 'Saved XGBoost pipeline'], ['Application version', '1.0']]]].map(([title, rows]) => <section key={String(title)} className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-semibold text-slate-800">{title}</h2><dl className="mt-4 divide-y divide-slate-100">{(rows as string[][]).map(([label, value]) => <div className="flex justify-between py-3 text-sm" key={label}><dt className="text-slate-500">{label}</dt><dd className="font-medium capitalize text-slate-700">{value}</dd></div>)}</dl></section>)}<button onClick={logout} className="rounded-lg border border-red-200 bg-white px-4 py-2.5 text-sm font-semibold text-red-700 hover:bg-red-50">Log out</button></div></div>; }
